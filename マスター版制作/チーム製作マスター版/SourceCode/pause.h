// =====================================================================================================================================================================
//
// ポーズの処理 [pause.h]
// Author : Sato Yoshiki
//
// =====================================================================================================================================================================
#ifndef _PAUSE_H_
#define _PAUSE_H_

// =====================================================================================================================================================================
// インクルードファイル
// =====================================================================================================================================================================
#include "main.h"
#include "scene.h"

// =====================================================================================================================================================================
// 前方宣言
// =====================================================================================================================================================================
class	CKeyboard;			// キーボードクラス
class	CXInputPad;			// ゲームパッドクラス
class	CPauseUI;			// ポーズUI

// =====================================================================================================================================================================
// 管理クラス
// =====================================================================================================================================================================
class CPause
{
public:

	enum PAUSE_MENU
	{
		PAUSE_NONE = 0,
		PAUSE_RESUME,							// 再開
		PAUSE_RESTART,							// リスタート
		PAUSE_TITLE,							// タイトル
	};

	CPause();												// コンストラクタ
	~CPause();												// デストラクタ
	/* メンバ関数 */
	HRESULT	Init();											// 初期化
	void	Uninit();										// 終了
	void	Update();										// 更新
	void	Draw();											// 描画
	void	SetPos(D3DXVECTOR3 pos);						// セットポジション

	/* 静的メンバ関数 */
	static CPause		*Create();							// ポーズの生成
	static int			GetNum() { return m_nNum; }			// ナンバーの取得

private:
	/* メンバ関数 */
	void	PauseConfig(CKeyboard *pKeyboard, CXInputPad *pXinputGpad);			// ポーズの設定
	void	IconMove(CKeyboard *pKeyboard, CXInputPad *pXinputGpad);			// アイコンの移動
	/* 静的メンバ変数 */
	static	int			m_nNum;								// ナンバー
	/* メンバ変数 */
	CPauseUI			*m_pPauseUI;						// ポーズUIのポインタ
};
#endif
