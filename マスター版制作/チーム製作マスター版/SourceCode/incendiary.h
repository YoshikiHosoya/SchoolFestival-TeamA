// =====================================================================================================================================================================
//
// 焼夷弾の処理 [Incendiary.h]
// Author : fujiwara masato
//
// =====================================================================================================================================================================
#ifndef _INCENDIARY_H_
#define _INCENDIARY_H_

// =====================================================================================================================================================================
// インクルードファイル
// =====================================================================================================================================================================
#include "main.h"
#include "bullet.h"

// =====================================================================================================================================================================
// 前方宣言
// =====================================================================================================================================================================
// =====================================================================================================================================================================
// 焼夷弾クラス
// =====================================================================================================================================================================
class CIncendiary : public CBullet
{
public:

	CIncendiary(OBJ_TYPE type);								// コンストラクタ
	~CIncendiary();											// デストラクタ

	/* メンバ関数 */
	HRESULT						Init();								// 初期化
	void						Uninit();							// 終了
	void						Update();							// 更新
	void						Draw();								// 描画
	void						DeleteBullet();						// 弾を消す処理
	void						BulletReaction(D3DXVECTOR3 rot);	// 弾の反応
	void						DebugInfo();						// デバッグ
	void						VelocityAttenuation();				// 移動量の減衰

	/* 静的メンバ関数 */
	static	CIncendiary			*Create(D3DXVECTOR3 rot);			// 焼夷弾の生成

private:
	/* 静的メンバ変数 */
	void						CalcBulletSpeed(D3DXVECTOR3 Target);// 焼夷弾の初速を求める

	/* メンバ変数 */
	D3DXVECTOR3					m_TargetPos;						// 目的の座標
	float						m_fSpeed;							// 弾の初速
	float						m_fGravityRate;						// 
	float						m_fRatio;							// 割合
	float						m_fMoveAttenuationRate;				// 移動量xの減衰の割合
	float						m_fVelocityBase;					// 速度割合
	float						m_fMoveResistance;					// 移動量の抵抗力

};
#endif