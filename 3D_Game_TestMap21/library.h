// =====================================================================================================================================================================
//
// ライブラリの処理 [library.h]
// Author : Sato Yoshiki
//
// =====================================================================================================================================================================
#ifndef _LIBRARY_H_
#define _LIBRARY_H_

#include "main.h"

// =====================================================================================================================================================================
// マクロ定義
// =====================================================================================================================================================================
#define PLAYER_PARTS				(13)				// プレイヤーのパーツ数

//---------------------------------------------
// 2D Int	( 整数を二つ格納する )
//---------------------------------------------
typedef struct INTEGER2
{
	int x, y;

	INTEGER2() {};
	INTEGER2(INT nx, INT ny)
	{
		x = nx;
		y = ny;
	};
}INTEGER2;

// =====================================================================================================================================================================
// ライブラリクラス
// =====================================================================================================================================================================
class CLibrary
{
public:

	//---------------------------------------------
	// Matrix
	//---------------------------------------------
	static	void	MatrixCalculation(D3DXMATRIX *mtxWorld, D3DXVECTOR3 rot, D3DXVECTOR3 pos);	// マトリックスの計算(位置、回転）
	static	void	BillboardMatrixCalculation(D3DXMATRIX * mtxWorld);	// ビルボードマトリックスの計算(位置、回転）

	//---------------------------------------------
	// Vertex
	//---------------------------------------------
	static	void	SetVertexObject(D3DXVECTOR3 *vtxMin, D3DXVECTOR3 *vtxMax, LPD3DXMESH	Mesh);	// 最小、最大頂点の設定

	//---------------------------------------------
	// ImGui
	//---------------------------------------------
	static	bool	SetObjectComboBox(int &nType);		// オブジェクトのコンボボックス
	static	bool	SetObject2DComboBox(int &nType);	// 2Dオブジェクトのコンボボックス
	static	bool	SetItemComboBox(int &nType);		// アイテムのコンボボックス

};
#endif
